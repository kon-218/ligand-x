// Stub module for jsdom/lib/jsdom/living/generated/utils
// This is required because paper.js tries to load this path for Node.js environments
// but it should never actually be used in browser builds
export const implForWrapper = () => null;
export const wrapperForImpl = () => null;
export default { implForWrapper, wrapperForImpl };
